{
  "name": "ai-minesweeper",
  "version": "2.0.0",
  "type": "module",
  "private": true,
  "description": "Human spoiled by AI — human-in → on → out of the loop",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "serve": "python3 -m http.server 8080"
  }
}
